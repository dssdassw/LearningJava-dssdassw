import java.util.Scanner;

public class MyInfo {
	public static void main(String[] args) {
		Scanner in = new Scanner(System.in);
		System.out.println("Yo MTV this is " + (in.nextLine()) + ", I be " + (in.nextInt()) + " yearz old an I be thinkin that the number " + (in.nextInt()) + " be pretty cool");
	}
}
